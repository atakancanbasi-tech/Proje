# ------------ Backend (Django) ------------
DJANGO_SECRET_KEY=replace-me
DEBUG=False
ALLOWED_HOSTS=localhost,127.0.0.1

# Veritabanı (örn. Postgres)
DATABASE_URL=postgres://user:password@localhost:5432/satis

# Statik/Medya (prod'da kullanıyorsanız)
STATIC_URL=/static/
MEDIA_URL=/media/

# Ödeme (örnek anahtar adları)
IYZICO_API_KEY=replace-me
IYZICO_SECRET=replace-me
PAYTR_MERCHANT_ID=replace-me
PAYTR_MERCHANT_KEY=replace-me
PAYTR_MERCHANT_SALT=replace-me

# E-posta (örn. SMTP)
EMAIL_HOST=smtp.example.com
EMAIL_PORT=587
EMAIL_HOST_USER=replace-me
EMAIL_HOST_PASSWORD=replace-me
EMAIL_USE_TLS=True

# ------------ Frontend (varsa Next.js/Vite) ------------
NEXT_PUBLIC_API_BASE=http://localhost:8000

# E-posta / SMTP
EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
EMAIL_HOST=smtp.your-provider.com
EMAIL_PORT=587
EMAIL_HOST_USER=your-user
EMAIL_HOST_PASSWORD=your-password
EMAIL_USE_TLS=1
DEFAULT_FROM_EMAIL=no-reply@your-domain.com